version: 2

updates:
  # The repo ships no package manifests — the skills' bundled scripts are
  # stdlib-only Python — so GitHub Actions is the only ecosystem to track.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      github-actions:
        patterns:
          - "*"
